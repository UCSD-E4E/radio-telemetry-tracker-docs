# Meeting Minutes

## FDS Debug

**Date:** Friday, April 25th, 2025  
**Time:** 4:12 PM - 4:32 PM Pacific Daylight Time  
**Location:** Zoom

---

### Attendees
- Tyler
- Vaibhav

---

### Agenda
1. Fix Compilation Errors with Drone FDS

---

### Discussion

#### Fix Compilation Errors with Drone FDS
- **Discussion Points:**
  - When running `poetry install` on a Mac, the process failed because the README's dependency install comands use Ubuntu-specific `apt` packages.
  - Recommended using a Docker container based on Ubuntu 24.04 (with Python 3.12.2) to encapsulate all system dependecies. Located an older Dockerfile int he repo and advised:
    - Installing only the system packages listed in the READ me via `RUN apt-get install ...`
    - Running `uhd_images.downloader`
    - Installing Poetry
    - Removing uncessary setup liens (lines 28-47) from exisitng Dockerfile
    - Simplifiyng the coressponding `docker-compose.yml` and `.devcontainer.json` (drop extra services, set u pvolume mounts and privilegs)
  - Create a new branch off `dev` (e.g., `feature/dev-container`) to integrate the Docker setup. Once validated, merge back into both the working branch (with Vaibhav’s config changes) and `dev`.
  - To allow testing without hardware, set config to:
    - GPS Type: `SIMULATED`
    - SDR Type: `GENERATED`
    - Operation Mode: `OFFLINE`
- **Action Items:**
  - Create dev container branch — *Assigned to:* Viabhav — *Due:* ASAP

---

### Next Meeting

N/A